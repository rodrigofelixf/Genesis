maior = 0 #contador
menor = 0 #contador 
for p in range(1, 6):
    peso = float(input(f'Peso da {p}ª pessoa: '))
    if p == 1: #se o primmeiro numero da variavel P dentro do range. O maior vai ser o primeiro
        # peso e o menor também porque eu quis. 
        maior = peso
        menor = peso
    else: #senão o "peso" que vier em seguida for maior, que o maior que eu ja escolhi porque eu quis em cima
        #o maior vai ser esse "peso"
        if peso > maior:
            maior = peso
        if peso < menor: # o mesmo vale para o menor peso se ele for o menor que o menor que escolhi em cima
            menor = peso
print(f'O maior peso lido foi de {maior}')
print(f'O menor peso lido foi de {menor}')